apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base/

namespace: mop

configMapGenerator:
  - name: env
    envs:
      - ./settings.env

secretGenerator:
  - name: env
    envs:
      - ./secrets.env
